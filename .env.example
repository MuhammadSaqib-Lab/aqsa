# Copy to .env.local for local development (Vite loads .env.local automatically
# and it's already covered by .gitignore).

# Base URL of the backend API, including the /api prefix.
VITE_API_URL=http://localhost:5000/api
